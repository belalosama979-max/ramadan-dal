# Ramadan Al-Rijal - المسابقة الرمضانية

مشروع ويب احترافي لمسابقات رمضان، مبني باستخدام React + Vite + TailwindCSS بتصميم رمضاني مميز وتقنيات Clean Architecture.

## 🛠 التقنيات المستخدمة

- **Framework**: React 18 (Vite)
- **Styling**: TailwindCSS (Custom Theme)
- **Language**: JavaScript (ES6+)
- **State Management**: React Context API
- **Persistence**: LocalStorage (Service Layer Validation)
- **Icons**: Lucide React
- **Fonts**: Tajawal (Google Fonts)

## 📂 هيكلة المشروع

```
src/
├── components/   # (Empty for now, using Tailwind directly)
├── context/      # GameContext (User & Quiz State)
├── hooks/        # Custom hooks (useGame)
├── layouts/      # Main Layout with Header & Background
├── pages/        # Application Pages (Landing, Question, Admin)
├── services/     # Core Business Logic (Storage, Question, Submission)
├── utils/        # Helper functions (Winner Logic)
├── App.jsx       # Main Router
└── main.jsx      # Entry Point
```

## 🚀 كيفية التشغيل

1. **تحميل الاعتماديات**:

   ```bash
   npm install
   ```

2. **تشغيل السيرفر المحلي**:

   ```bash
   npm run dev
   ```

3. **بناء النسخة النهائية**:
   ```bash
   npm run build
   ```

## 🧠 شرح المنطق (Core Logic)

### 1. تحديد الفائز (Winner Logic)

يتم حساب الفائز في `src/utils/winnerUtils.js` بناءً على المعايير التالية:

1. جلب جميع المشاركات للسؤال المحدد.
2. تصفية الإجابات **الصحيحة فقط** (`isCorrect === true`).
3. ترتيب الإجابات زمنياً **تصاعدياً** (الأقدم أولاً).
4. صاحب أول إجابة صحيحة هو الفائز.

### 2. التحقق من الوقت (Time Validation)

- يتم تخزين `startTime` و `endTime` لكل سؤال بصيغة ISO.
- في `QuestionService.js`، يتم التحقق مما إذا كان الوقت الحالي يقع ضمن النطاق الزمني للسؤال لعرضه.
- في `SubmissionService.js`، يتم رفض أي إجابة تصل بعد `endTime`.

### 3. الحماية من التكرار (Anti-Spam)

- يتم "تطبيع" (Normalize) اسم المستخدم (تحويل لأحرف صغيرة + إزالة المسافات).
- قبل الحفظ، يتم التحقق مما إذا كان هذا الاسم قد أجاب على نفس السؤال مسبقاً.
- يتم ذلك محلياً في `localStorage` ولكن الآلية جاهزة للعمل مع Backend حقيقي.

## 🛡 الأمان (Security Considerations)

- يتم تعقيم المدخلات (Sanitization) قبل الحفظ.
- التحقق يتم في طبقة الـ Service وليس فقط في الواجهة.
- استخدام `uuid` لإنشاء معرفات فريدة للأسئلة والمشاركات.

---

تم التطوير بواسطة: **Antigravity** لصالح **نادي الرجال**.
رمضان كريم 🌙
